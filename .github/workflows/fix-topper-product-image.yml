name: Historical topper image patch (retired)
on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm patch is retired
        run: echo 'Current product-detail.js already uses local mattress photos.'
